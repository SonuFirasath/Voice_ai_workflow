TENANT_ID=
CLIENT_ID=
CLIENT_SECRET=

TARGET_EMAIL=FirasathG@chimeratechnologies.com
TIME_ZONE=Asia/Kolkata

PORT=3000

# web-test-mode branch only: lets verify_pin resolve a caller identity when
# VAPI's dashboard test call provides no phone number. Never set this on a
# branch/deployment that handles real phone calls.
ENABLE_WEB_TEST_MODE=false
TEST_CALLER_EMAIL=
